# Apply drafts

The apply assistant writes form Q&A here after each `/career-ops apply` session.

**Filename:** `{report-number-zero-padded}.md` (e.g. `033.md` for report #33)

**View in web UI:** Applications → **View apply** (after the agent saves the file).

**Cursor:** Use one pinned chat for all career-ops work; paste the **Copy apply** prompt from the web UI per job.
